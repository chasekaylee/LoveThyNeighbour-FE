// import { selectAssistancePageDomain } from '../selectors';

describe('selectAssistancePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
